name: demo-workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      name:
        description: "cluster-name"
        type: string
        required: true
        default: "GKE-cluster"
      version:
        description: "cluster-version"
        type: string
        required: true
        default: "1.33"

permissions: {}

jobs:
  job1:
    name: job1
    runs-on: ubuntu-latest
    steps:
      - name: Print cluster name
        run: echo "${{ github.event.inputs.name || 'GKE-cluster' }}"
      - name: Print cluster version
        run: echo "${{ github.event.inputs.version || '1.24' }}"

  job2:
    name: job2
    needs: job1
    runs-on: windows-latest
    steps:
      - name: Print cluster name
        run: echo "${{ github.event.inputs.name || 'GKE-cluster' }}"
      - name: Print cluster version
        run: echo "${{ github.event.inputs.version || '1.24' }}"
